#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n, st;
    vector<pair<int,int>> vec;
    while(cin >> n >> st){
        vec.push_back({n,st});
    }
    for(int i = 0; i < vec.size(); i++){
        while()
    }


    return 0;
}
